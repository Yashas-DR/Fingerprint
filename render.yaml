services:
  - type: web
    name: blood-group-predictor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ap:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
